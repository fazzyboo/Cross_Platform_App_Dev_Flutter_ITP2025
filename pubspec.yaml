name: cross_platform_app_dev_flutter_itp2025
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.2.1 # For making API calls to LM Studio
  shared_preferences: ^2.2.3 # For local key-value storage
  video_player: ^2.8.6 # For playing background video animation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/videos/Lines-moving-right.mp4
